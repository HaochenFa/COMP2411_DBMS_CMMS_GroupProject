# Copy this file to `.env` and adjust the values for your local setup

# This file is intentionally committed so you can see which variables are needed

# MySQL connection settings

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=cmms_db
